# セキュリティポリシー (Security Policy)

本ドキュメントは、rag-support-assist に対する脆弱性を発見された方に向けた**報告手順**と**開示方針**をまとめたものです。ご報告に感謝します。

## 1. 対応バージョン

現時点で公式にセキュリティ修正を提供する対象は、リポジトリのデフォルトブランチ (`main`) の最新コミットです。特定のリリースタグ / バージョン系列を維持していない場合、常に最新の `main` に対して修正がリリースされます。

| バージョン | サポート状態         |
| ---------- | -------------------- |
| `main`     | ✅ セキュリティ修正対象 |
| 過去タグ   | ❌ 提供なし（最新への更新を推奨） |

## 2. 脆弱性の報告方法

**公開の GitHub Issue は使用しないでください。** 悪用リスクを避けるため、以下の非公開経路を利用してください。

- **第一経路 (推奨):** GitHub の **Private Vulnerability Reporting** を利用してください。
  - リポジトリ `Security` タブ → `Report a vulnerability` から匿名 / 認証済みいずれでも報告できます。
- **フォールバック:** 上記が利用できない場合、リポジトリオーナー宛に、公開されているコンタクト経路 (プロフィール / GitHub Discussions) から「Security report を送りたい」旨のみを連絡し、指定された経路で本文を送付してください。

いずれの経路でも、報告本文には次を含めてください（判明している範囲で構いません）:

- 影響を受けるコンポーネント (例: `backend/` / `frontend/` / 特定のエンドポイント)
- 影響を受けるバージョン / コミット SHA
- 再現手順（可能なら最小の PoC）
- 想定される影響範囲 / 深刻度の見積もり
- 提案する緩和策や修正方針（任意）

## 3. 対象範囲

**対象内 (In scope):**

- API 認証・認可の不備、権限昇格
- 認証情報 / API キー / 秘匿情報の漏洩
- SQL / NoSQL / コマンド / パス インジェクション
- SSRF / RCE / 任意ファイル読み取り書き込み
- クロスサイトスクリプティング (XSS) / CSRF
- ベクトルストア / データベース経由のデータ漏洩
- RAG 固有: **プロンプトインジェクション**によりシステム外に情報が漏れる / 意図しない操作が誘発される
- ドキュメントアップロード経路の悪用（サイズ制限バイパス、悪意ある形式のパース等）
- 依存パッケージの既知脆弱性の未パッチ

**対象外 (Out of scope):**

- 実運用インフラを持たない本リポジトリのローカルコード外で発生する事象
- 公開されているサンプルデータの内容そのもの
- レート制限の欠如それ自体（DDoS 想定）
- 自動スキャナが出力する低優先度の報告（説明・再現性のないもの）

## 4. 応答目標時間 (SLA 目安)

以下はコミュニティベースの運用における「目安」です。厳密な保証ではありません。

| フェーズ           | 目安                                |
| ------------------ | ----------------------------------- |
| 受領確認           | 5 営業日以内                        |
| 初期トリアージ     | 10 営業日以内                       |
| 修正案提示または解決 | 深刻度と複雑さに応じて 30〜90 日   |

進捗は Private Vulnerability Reporting のスレッドで随時共有します。

## 5. 開示ポリシー

- 修正リリース後、GitHub Security Advisories (GHSA) を発行して同一スレッドで開示します。
- 報告者が匿名希望でない限り、Advisory に謝辞として名前 / ハンドルを記載します。
- 修正リリース前の一次情報（詳細・PoC）の公開は差し控えてください（責任ある開示）。
- 独立して同一脆弱性が公表された場合、確認取れ次第当方の Advisory も速やかに公開します。

## 6. RAG / LLM 特有の注意事項

本プロジェクトは RAG (Retrieval-Augmented Generation) を利用しています。以下は脆弱性としてのみならず、運用上の注意点としても認識しています。

- **プロンプトインジェクション:** 検索対象ドキュメントや検索結果に含まれる指示文が LLM の応答を操作する可能性
- **データ持ち出し:** 検索結果として返される内容がユーザに露出することの是非（アクセス制御の伝播）
- **モデル / API キー漏洩:** LLM プロバイダの API キー漏洩は本ポリシーの対象内

これらに関連する報告は Advisory の対象とし、同 SLA で対応します。

## 7. 謝辞

セキュリティコミュニティのご協力に感謝します。責任ある開示に基づいてご報告くださった方々へ、修正リリース時に Advisory 内で謝辞を記載します。
